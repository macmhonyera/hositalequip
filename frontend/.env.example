# Backend API URL — set to your deployed backend
# In development, the Vite proxy handles /api/v1 requests
# In production, this MUST point to the full backend URL
VITE_API_URL=http://localhost:3001/api/v1
